{
  "routes": [
    {
      "method": "GET",
      "path": "/terms",
      "handler": "Term.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/terms/count",
      "handler": "Term.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/terms/:_id",
      "handler": "Term.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/terms",
      "handler": "Term.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/terms/:_id",
      "handler": "Term.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/terms/:_id",
      "handler": "Term.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
